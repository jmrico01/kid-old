#pragma once

#include <km_common/km_defines.h>

#define APP_KID

global_var const char* APP_NAME = "kid";
global_var const uint64 PERMANENT_MEMORY_SIZE = MEGABYTES(64);
global_var const uint64 TRANSIENT_MEMORY_SIZE = GIGABYTES(2);